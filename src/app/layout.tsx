import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Crypto Dashboard | Real-Time Market Insights",
  description:
    "A comprehensive, real-time dashboard for cryptocurrency market capitalization, prices, and trading volume, powered by CoinGecko.",
  keywords: [
    "crypto",
    "bitcoin",
    "ethereum",
    "dashboard",
    "market cap",
    "coingecko",
  ],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
